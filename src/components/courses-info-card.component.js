import React from "react";
import { View } from "react-native";
import { Card } from "react-native-paper";

export default function CoursesInfoCard(courses = {}) {
  const {} = courses;
  return <Card></Card>;
}
